{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/hiro/:path*",
      "destination": "/api/hiro?path=:path*"
    },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
